
function exp() {console.log("qq")
	["Wx"].forEach(tag => {
	const el = document.getElementById(`operations-tag-${encodeURIComponent(tag)}`);
    if (el && !el.classList.contains('is-open') && true) {
	el.click();
	}
	///**
    });
};


